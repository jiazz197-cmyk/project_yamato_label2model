# 交接工作手册

> 最后更新：2026-08-12
> 作者：[填写]

---

## 一、项目概述

**Yamato** — 基于 Label Studio 1.24 (Apache-2.0) 的数据标注平台。私有化部署，保留标注、数据集导出、用户鉴权功能。

| 维度 | 详情 |
|------|------|
| 项目目录 | `D:\Code\project_yamato_label2model` |
| 后端框架 | Django 5.1 + DRF |
| 前端框架 | React（Nx Monorepo）+ Yarn 1.x |
| Python 环境 | `.venv`（Python 3.12.13），Poetry 管理依赖 |
| Node.js | v26.5.1，Yarn 1.22.22（在 conda base 中） |
| 数据库 | PostgreSQL（生产）+ SQLite（本地开发） |
| 存储 | MinIO（S3 兼容）+ Redis |
| 当前分支 | `codex/zh-localization` |
| Git 仓库 | 私有仓库 |

---

## 二、每次工作前必读

### 2.1 激活环境

在项目根目录打开终端，**先激活 Python venv**（注意不是 conda base）：

```powershell
.venv\Scripts\activate
```

### 2.2 启动开发服务器

```powershell
.venv\Scripts\activate
python label_studio/manage.py runserver 0.0.0.0:8080
```

访问 http://localhost:8080

### 2.3 前端构建

> ⚠️ **注意**：运行 `yarn` 命令时不要在 `.venv` 环境下，因为 Yarn 在 conda base 中。可以开两个终端，或者用完整路径。

```powershell
# 方法一：新终端（不激活 .venv）
cd D:\Code\project_yamato_label2model\web
yarn build

# 方法二：用完整路径
C:\Users\Catking\miniconda3\yarn.cmd build
```

### 2.4 数据库迁移（首次/依赖变更时）

```powershell
.venv\Scripts\activate
python label_studio/manage.py migrate
python label_studio/manage.py collectstatic --no-default-ignore
```

---

## 三、2026-08-12 工作记录

### 完成事项

#### 1. 登录页吉祥物清理
- 删除登录/注册页的 Heidi（负鼠）吉祥物元素
- 移除 `user_tips.html`（tips 面板）及 `heidi-tip.svg` CSS 装饰图
- 旧版登录模板中 `opossum_hanging.svg` 替换为 `yamato_logo.png`
- 旧版注册模板中移除 Heidi 图片

#### 2. 全面中文化（i18n）
**登录/注册页（Django 模板）：**
- `users/templates/users/new-ui/user_base.html` — 副标题中文化
- `users/templates/users/new-ui/user_login.html` — 7 处（登录、邮箱地址、密码等）
- `users/templates/users/new-ui/user_signup.html` — 9 处（注册、密码、来源选择等）
- 旧版同路径模板同步翻译

**用户设置页（Django 模板）：**
- `users/templates/users/user_account.html` — 17 处标签中文化（头像、个人信息、令牌、组织信息等）

**表单验证消息：**
- `users/forms.py` — 8 处（邮箱或密码错误、邮箱地址过长、用户名已存在等）
- `users/functions/common.py` — 修复硬编码 `'Other'` → 引用 `FOUND_US_ELABORATE` 常量

**Heidi Tips（主页右上角提示）：**
- `core/views.py` — `heidi_tips` API 改为读取本地文件（不再从 GitHub 拉取）
- `web/.../liveContent.json` — 25 条提示全部翻译为中文

**Account Settings（React SPA 账户设置页）：**
- `AccountSettings.tsx` — 页面标题 "My Account" → "我的账号"
- `sections/index.tsx` — 5 个菜单项 + 快捷键描述
- `sections/PersonalInfo.tsx` — 表单标签、按钮、错误提示
- `sections/EmailPreferences.tsx` — 订阅描述文本
- `sections/MembershipInfo.tsx` — 8 种角色名 + 6 个信息标签
- `sections/PersonalAccessToken.tsx` — 3 个标签 + 按钮文本
- `sections/PersonalJWTToken.tsx` — 17 处英文文本

#### 3. 代码审查
完成了整个项目的全面代码审查，形成审查报告。主要发现：
- `.env` 凭据已入库（私有仓库风险）
- `SECRET_KEY` 覆盖逻辑问题
- `DEBUG=True` 和 `CORS_ALLOW_ALL_ORIGINS=True` 默认值风险
- `tasks/` 迁移文件过多（61 个）
- 云存储已删除（迁移 `0023_remove_cloud_storage_tables.py`）

### Git 提交

```
62a5621 refactor(i18n): 登录页/用户设置/Heidi提示全面中文化
19 files changed, 238 insertions(+), 292 deletions(-)
```

### 待处理事项（留给下一位）
- [ ] 生产环境确认 `DEBUG=False`、`CORS_ALLOW_ALL_ORIGINS=False`
- [ ] 修复 `SECRET_KEY` 覆盖逻辑（`.env` 中的值被 `label_studio.py:17` 覆盖）
- [ ] `.env` 考虑从 Git 中移除，改为 `.env.example` 模板
- [ ] Squash `tasks/` 的 61 个迁移文件
- [ ] `static_build/css/login.css` 需要运行 `collectstatic` 刷新

---

## 四、项目关键文件速查

| 用途 | 路径 |
|------|------|
| 环境配置 | `.env` |
| 开发指南 | `AGENTS.md` |
| 开发备忘 | `开发备忘.md` |
| 后端设置 | `label_studio/core/settings/base.py` |
| 覆盖设置 | `label_studio/core/settings/label_studio.py` |
| 登录模板 (new-ui) | `label_studio/users/templates/users/new-ui/` |
| 登录模板 (旧) | `label_studio/users/templates/users/` |
| 用户设置页 | `label_studio/users/templates/users/user_account.html` |
| 表单验证 | `label_studio/users/forms.py` |
| Heidi Tips API | `label_studio/core/views.py` (heidi_tips 函数) |
| Heidi Tips 内容 | `web/apps/labelstudio/src/components/HeidiTips/liveContent.json` |
| 账户设置 React | `web/libs/app-common/src/pages/AccountSettings/` |
| 前端主应用 | `web/apps/labelstudio/` |
