# 开发备忘

> 最后更新：2026-08-11

---

## 环境速查

| 组件 | 路径/版本 |
|------|----------|
| Python 虚拟环境 | `.venv`（Python 3.12.13） |
| Django | 5.1.15 |
| Node.js | v26.5.1 |
| Yarn | 1.22.22 |
| npm registry 镜像 | `https://registry.npmmirror.com` |

---

## 每次开始工作前

在项目根目录 `D:\Code\project_yamato_label2model` 下打开终端，**必须先激活 venv**：

```powershell
.venv\Scripts\activate
```

激活后终端提示符会变成 `(.venv) PS D:\Code\project_yamato_label2model>`。

> ⚠️ 不要用 conda 的 `(base)` 环境，那里没有 Django。

---

## 安装与构建

### 前端（首次/依赖变更时）

```powershell
cd web
yarn install --frozen-lockfile   # lockfile 已是 Yarn 1.x 格式，秒级完成
yarn build
cd ..
```

### 后端（首次）

```powershell
.venv\Scripts\activate
poetry install                   # 如果 poetry 不可用，pip install -r requirements.txt
python label_studio/manage.py migrate
python label_studio/manage.py collectstatic --no-default-ignore
```

### 启动开发服务器

```powershell
.venv\Scripts\activate
python label_studio/manage.py runserver 0.0.0.0:8080
```

访问 http://localhost:8080

---

## 2026-08-11 修复记录

### 问题 1：yarn install 极慢（20+ 分钟卡 [1/5]）

**原因**：
- `yarn.lock` 是 Yarn Berry v4 格式，但安装的是 Yarn 1.22.22，无法解析
- 默认 registry `registry.yarnpkg.com` 国内访问极慢
- `nx` 包的 postinstall 脚本在 Windows 上永久挂起

**修复**：
- 切换 registry 到 `https://registry.npmmirror.com`（`yarn config set`）
- 删除 Berry 格式 lockfile，重新生成 Yarn 1.x 格式 lockfile
- `@biomejs/biome` 固定为 `2.5.7`（国内镜像缺 `2.5.8` 的 win32 二进制）
- 用 `--ignore-scripts` 跳过 postinstall 安装

**当前状态**：`node_modules` 1437 个包已装好，可直接 `yarn build`。

### 问题 2：找不到 Django 模块

**原因**：终端在 conda `(base)` 环境（Python 3.14），Django 装在项目 `.venv` 里。

**修复**：每次工作前先 `.venv\Scripts\activate`。
